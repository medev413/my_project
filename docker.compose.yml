version: '3.8'

services:
  website:
    build: .                  # build from Dockerfile in current dir
    container_name: my-website
    ports:
      - "8080:80"             # host:container
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html  # live reload
    restart: always           # auto restart if container crashes